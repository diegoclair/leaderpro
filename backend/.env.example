# Copy this file to .env and fill in your actual values
# cp .env.example .env

# AI Provider API Keys (REQUIRED for AI features)
# These override the values in config-local.toml via Viper's automatic mapping:
# ai.openai.api-key -> AI_OPENAI_API_KEY
# ai.anthropic.api-key -> AI_ANTHROPIC_API_KEY

AI_OPENAI_API_KEY=your-openai-api-key-here
AI_ANTHROPIC_API_KEY=your-anthropic-api-key-here

# Environment Configuration
ENV=local

# Note: All other configuration values are loaded from config-local.toml
# You can override any config value by setting the corresponding environment variable
# using the pattern: SECTION_SUBSECTION_KEY (all uppercase, replacing . and - with _)
#
# Examples:
# DB_MYSQL_HOST=mysql-container
# CACHE_REDIS_HOST=redis-container
# APP_PORT=5000